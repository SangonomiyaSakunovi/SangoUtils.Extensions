using SangoScripts_Server;
using SangoScripts_Server.Cache;
using SangoScripts_Server.Net;

namespace SangoUtils_Server
{
    public class LobbyPlayerEntity(string entityID, Transform transform, ClientPeer clientPeer) : PlayerEntity(entityID, transform, clientPeer)
    {

    }
}
